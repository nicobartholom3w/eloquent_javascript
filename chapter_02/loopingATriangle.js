
let block = "#";

for (let count = 1; count <= 7; count++){
	let line = block.repeat(count);
	console.log(line);
}


/* Book Answer

for (let line = "#"; line.length < 8; line += "#")
  console.log(line);

  */
 